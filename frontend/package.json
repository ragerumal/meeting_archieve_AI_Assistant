{
  "name": "zoom-rag-frontend",
  "version": "1.0.0",
  "description": "Frontend for Zoom RAG Insight Engine",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "axios": "^1.6.0",
    "aws-amplify": "^6.0.0",
    "aws-amplify-react-auth": "^3.0.0",
    "zustand": "^4.4.0",
    "react-markdown": "^9.0.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && aws s3 sync ./build s3://your-s3-bucket-name --delete"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
